#!/usr/bin/env bash
# Sample desktop environment config copied to ~/.termux-desktop/env.sh by core module.
# Keep this file in version control so defaults are easy to audit.

export DISPLAY=:0
export PULSE_SERVER=127.0.0.1
export XDG_RUNTIME_DIR="${TMPDIR:-/data/data/com.termux/files/usr/tmp}"
